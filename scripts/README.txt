- On Windows use MinGW / Msys

- Copy scripts dir to a desired <project> name

1. In make_cscope_ctags.sh fill in:

- the base dir: basedir=<path to source code>
- the sub dir: subdir=<subdir name>
  this sub dir will be use for project name and the generated *.bat *.sh files

2. start the StartCodeQuery.sh script

